from flask import Flask # Import the Flask class so that we can make the app
app = Flask(__name__) # Defines the app so that it's available for all our folders and files in the project

# We'll add a secret key next week here.